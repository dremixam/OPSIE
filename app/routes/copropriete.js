var coproprieteModel = require('../models/copropriete');

module.exports = function(socket) {
  socket.on('list-copropriete', function (message) {

  });
  socket.on('get-copropriete', function (message) {

  });
};
